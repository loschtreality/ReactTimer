var React = require('react');

var Timer = React.createClass({

  render: function() {
    return (
      <div>
        <p>
          Timer component rendered
        </p>
      </div>
    );
  }

});

module.exports = Timer;
